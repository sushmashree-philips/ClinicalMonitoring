import { ProgressStepDirectiveDirective } from './progress-step-directive.directive';

describe('ProgressStepDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressStepDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
